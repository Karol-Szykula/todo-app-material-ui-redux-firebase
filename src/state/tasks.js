

const INITIAL_STATE = {
    tasks: [],
    filterText: '',
    chosenFilter: 'All',
    newTaskText: ''

}

export default (state = INITIAL_STATE, action) => {
    switch (action.type) {

        default:
            return state
    }
}